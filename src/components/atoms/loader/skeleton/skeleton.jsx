import React from 'react';
import Styles from '@/components/atoms/loader/skeleton/skeleton.module.css';

const Skeleton = () => {
    return (
        <div className={Styles.skeleton}>
        </div>
    );
};

export default Skeleton;